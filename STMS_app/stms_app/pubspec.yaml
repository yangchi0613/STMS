name: stms_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # 基礎套件
  intl: ^0.20.0
  table_calendar: ^3.0.9
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.3
  
  # Firebase 套件 (保持最新即可)
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  
  # [關鍵] 通知套件：鎖定在 v17 版本，解決紅字問題
  flutter_local_notifications: 17.2.3
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true